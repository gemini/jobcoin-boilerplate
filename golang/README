# Golang Jobcoin
Simple base project for the Jobcoin project using Golang. It accepts return address as arguments and prints
out a deposit address to the user for them to send their funds to.

The rest of the application is left unimplemented.


### Clean + Deps + Build
    `make all`

### Clean
    `make clean`

### Deps
    `make deps`

### Build
    `make build`

### Test
    `make test`

### Run
    `./bin/mixer --addresses=bravo,tango,delta`
