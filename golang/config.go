package jobcoin

// Configuration defines a base minimum configuration for the jobcoin mixer
const (
	BaseURL             = "https://jobcoin.gemini.com/CHANGEME/api"
	AddressesEndpoint   = BaseURL + "/addresses"
	TransactionEndpoint = BaseURL + "/transactions"
)
